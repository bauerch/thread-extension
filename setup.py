from setuptools import setup

# Metadata goes in setup.cfg. These are here for GitHub's dependency graph.
setup(
    name="worker-threads",
    install_requires=[
        "transitions>=0.8.11"
    ]
)
